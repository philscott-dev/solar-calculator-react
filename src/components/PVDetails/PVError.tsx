import styled from '@emotion/styled'

export default styled.p`
  position: absolute;
  bottom: -8px;
  left: 24px;
  font-weight: normal;
  font-size: 12px;
  line-height: 15px;
  color: ${({ theme }) => theme.color.red[300]};
`
